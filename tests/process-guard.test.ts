import { describe,expect,it } from "vitest";import { runRestricted } from "../src/process-guard.js";describe("process guard",()=>{it("captures bounded process output",async()=>{await expect(runRestricted(process.execPath,["-e","process.stdout.write('ok')"],{timeoutMs:1000,maxOutputBytes:100})).resolves.toEqual({stdout:"ok",stderr:""})});it("stops excessive output",async()=>{await expect(runRestricted(process.execPath,["-e","process.stdout.write('x'.repeat(1000))"],{timeoutMs:1000,maxOutputBytes:10})).rejects.toThrow("output limit")});it("times out hung processes",async()=>{await expect(runRestricted(process.execPath,["-e","setTimeout(()=>{},10000)"],{timeoutMs:20,maxOutputBytes:100})).rejects.toThrow("timed out")})});