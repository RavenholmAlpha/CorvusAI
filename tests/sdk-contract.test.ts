import { describe,expect,it } from "vitest";import { CORVUS_PLUGIN_API_VERSION } from "../src/plugins.js";import * as sdk from "../src/index.js";describe("public SDK contract",()=>{it("exports stable plugin and capability surfaces",()=>{expect(CORVUS_PLUGIN_API_VERSION).toBe(1);expect(sdk.PluginRuntimeManager).toBeTypeOf("function");expect(sdk.PluginManagementService).toBeTypeOf("function");expect(sdk.createToolManifest).toBeTypeOf("function");expect(sdk.BundleService).toBeTypeOf("function");expect(sdk.MemoryEngine).toBeTypeOf("function")})});